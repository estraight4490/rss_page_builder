{
  "name": "simple-rss",
  "version": "1.0.0",
  "description": "A simple rss that generates a static HMTL file with links",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "config": "^3.3.8",
    "feedparser": "^2.2.10",
    "iconv-lite": "^0.6.3",
    "node-fetch": "^2.6.6"
  }
}
